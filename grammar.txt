expr        : term ((PLUS|MINUS) term)*

term        : factor ((MUL|DIV) factor)*

power       : factor (POWER factor)*

# most priority
factor      : INT|FLOAT
            : (PLUS|MINUS) factor
            : LPAREN expre RPAREN